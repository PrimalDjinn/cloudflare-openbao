# Build outputs
node_modules
dist

# Env files
.env
.env.*
!.env.example
.dev.vars
!.dev.vars.example

# Wrangler
.wrangler

# Git
.git
.gitignore

# Docker
Dockerfile
.dockerignore

# Don't ignore the cloned repo — it's needed in the build
!.ciao-repo
